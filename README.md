## Init project

Up docker-compose
```
$ docker-compose up -d
```

Run this make command to import the initial UCL data
```
$ make import_football_data
```

## Stop project

```
$ docker-compose down
```